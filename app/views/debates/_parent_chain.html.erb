<div id="parents">
  <% if debate.debate == nil then %>
    <p <%= debate.generate_style %> >
      <h1><%= link_to debate.proposition, debate_path(debate) %></h1>
    </p>
  <% else %>
    <%= render :partial => "parent_chain", :locals => { :debate => debate.debate } %>
    <p <%= debate.generate_style %> >
      <b>Content:</b>
      <%= debate.content %>
      <%= link_to "show", debate_path(debate) %>
    </p>
  <% end %>
</div>
